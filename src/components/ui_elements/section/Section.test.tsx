import { render } from '../../../tests/unit_and_integration/test-utils';
import Section from './Section';

const setup = (children: React.ReactNode) =>
  render(<Section children={children} />);

describe('Tests Section component', () => {
  it('gets displayed', () => {});

  it('renders with children', () => {});
  it('matches snapshot', () => {});
});
